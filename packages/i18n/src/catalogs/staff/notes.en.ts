/**
 * `staff.notes.*` — the staff phone’s notes on new items.
 * Owned by lane H (docs/design/protocols/build-contracts-2026-09-23.md §4).
 * Mirror every key in notes.ar.ts.
 */
export const staffNotesEn = {} as const;
