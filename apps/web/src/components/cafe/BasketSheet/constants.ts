/** Order-level note cap (mirrors the draft persistence in lib/cafe/basket.ts). */
export const ORDER_NOTE_MAX = 200;
/** Quantity stepper bounds, same as the item sheet. */
export const QTY_MIN = 1;
export const QTY_MAX = 99;
