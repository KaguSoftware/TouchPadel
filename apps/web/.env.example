# Copy to .env.local for local dev (values from `supabase start` output).
# Staging/production values live in Vercel env settings — never commit real keys.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=replace-with-local-anon-key
